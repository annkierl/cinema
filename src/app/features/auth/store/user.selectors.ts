import { createFeatureSelector, createSelector } from '@ngrx/store';
import { AppState } from '../../home';
import { UserState } from '../../home/AppUser.interface';

// export const selectAuth = (state: AppState) => state.User;
// export const selectAccountType = createSelector(selectAuth, (state: UserState) => state.role);
